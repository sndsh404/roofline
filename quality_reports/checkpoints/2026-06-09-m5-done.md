# Checkpoint, 2026-06-09, M5 done (CPU), all v0 milestones complete

## Where I stopped
M5 is done in its CPU-honest form. All six milestones (M0 through M5) are
ticked in CLAUDE.md. assess 100/100, 31 tests green.

The preregistration discipline was followed to the letter and the ledger now
proves it:
- pilot (seed 42, exploratory, NOT a claim): fused MLP beat naive at every
  shape, 1.25x to 1.57x at s=2048 d=128.
- prereg `mlp-s43-001` (fresh seed 43, threshold 1.10, numerics gate 1e-5):
  recorded 1.245x, err exactly 0.0. claim_met true.
- prereg `attention-s43-002` (threshold 1.0): recorded 1.209x, err 6.74e-6.
  claim_met true.
- `roofline replay` re-ran both from their committed configs: mlp 1.279x,
  attention 1.118x, errors reproduce bit-exactly, claims hold. History lives
  in `ledger/wal.jsonl` (committed).
- accountant at the MLP headline shape: naive moves 19.9 MB, fused 3.1 MB, a
  6x byte cut. CPU is compute-bound so the wall-clock win is modest; the A100
  half of the criterion (beat jax.lax.ragged_dot) stays deferred, recorded,
  not faked.

scripts/assess.py now has check_ledger(): it reads the WAL, records each
run's latest metric as a number, and a claim_met false is a P1 that blocks
the push. So the headline numbers are regression-guarded from now on.

Docs are current: CLAUDE.md tracker + build commands + repo map match
reality; README sections 6, 12, 15, 17 updated with the recorded numbers and
run ids. No long dashes anywhere; figures are the hand-drawn black and white
style with Humor Sans (docs/fonts/HumorSans.ttf).

## Next actions (no required work left in v0; in preference order)
1. SRAM capacity constraint in rl-cost (forces tiling where the fused region
   cannot fit; the project's rule applied to its own documented gap). Done
   when: with the constraint active, the extractor stops choosing whole-graph
   fusion for shapes whose working set exceeds Device sram_bytes, and a test
   pins it.
2. Exact min-cost DAG extraction via ILP if a CBC solver becomes available;
   compare against the greedy extractor's picks.
3. GPU work when hardware exists: Pallas/Triton emission, A100 calibration
   (M1), ragged_dot comparison (M5), recording the predicted vs measured gap.

## Environment / resume
- Same as previous checkpoint (Rust 1.94 gnu, python 3.14). Remote
  `https://github.com/sndsh404/roofline.git`, system git config (sndsh404),
  never set author or trailers, push after every meaningful change.
- Resume: read CLAUDE.md, README, this file. `python scripts/assess.py
  --start`, `cargo test --workspace` (expect 31), then pick from the list
  above or whatever the user asks.
